// Khai báo mở rộng bảng màu (Theme Colors) riêng cho ứng dụng Lorebook
tailwind.config = {
    theme: {
        extend: {
            colors: {
                'lore-dark': '#0E1210',    // Màu nền tối chủ đạo
                'lore-card': '#171C19',    // Màu nền của các box, card nội dung
                'lore-green': '#A3E635'    // Màu xanh lục neon thương hiệu của Lorebook
            }
        }
    }
}
